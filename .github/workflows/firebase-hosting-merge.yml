# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
'on':
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: self-hosteddd
    steps:
      - uses: actions/checkout@v2
      - run: npm ci && npm run build && npm run start-serv
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_STEEL_SIGNIFIER_329004 }}'
          channelId: live
          projectId: steel-signifier-329004
